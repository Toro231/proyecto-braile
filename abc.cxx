
#include "abc.h"



void funcion_a(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
    }  
  }
}
void funcion_b(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
    }  
  }
}
void funcion_c(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_d(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <=2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_e(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_f(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_g(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
       if (j == 1 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_h(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_i(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      j=pos;
      myservo.write(pos);//le dice al servo en que posicion ponerse
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_j(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_k(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      j=pos;
      myservo.write(pos);//le dice al servo en que posicion ponerse
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_l(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_m(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 2; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_n(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_o(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }  
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_p(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_q(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_r(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      j=pos;
      myservo.write(pos);//le dice al servo en que posicion ponerse
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_s(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_t(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
    }
  }
}
void funcion_u(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
      if (j == 2 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_v(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
      if (j == 2 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_w(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_x(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 0 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
      if (j == 2 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_y(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      myservo.write(pos);//le dice al servo en que posicion ponerse
      j=pos;  
      if (j == 0 && i == 0){
        marcar();
      }
      if (j == 1 && i == 0){
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
      if (j == 2 && i == 1){
        marcar();
      }
    }
  }
}
void funcion_z(){
  for(i=0 ; i<1 ; i++){//la i son las pociciones del pap
    funcion_papel();
    for(pos = 0; pos <= 3; pos += 1){//son las pociciones del microservo
      j=pos;
      myservo.write(pos);//le dice al servo en que posicion ponerse
      if (j == 0 && i == 0){
        j=pos;
        marcar();
      }
      if (j == 1 && i == 1){
        marcar();
      }
      if (j == 2 && i == 0){
        marcar();
      }
      if (j == 2 && i == 1){
        marcar();
      }
    }
  }
}
